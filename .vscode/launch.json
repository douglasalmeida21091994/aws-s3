{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Listen for Xdebug (Docker PHP 7.2)",
            "type": "php",
            "request": "launch",
            "port": 9000,
            "pathMappings": {
                "/var/www/html": "${workspaceFolder}/src"
            }
        }
    ]
}